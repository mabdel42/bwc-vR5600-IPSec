---
# Action definition metadata
name: "specify_ike_version"
description: "Specifies the IKE version of an IKE group"
pack: vR5600_IPSec
# `runner_type` has value `action-chain` to identify that action is an ActionChain.
runner_type: "python-script"

# `entry_point` path to the ActionChain definition file, relative to the pack's action directory.
entry_point: "specify_ike_version.py"

# Parameters of the action chain.
parameters:
    deviceIP:
        type: string
        description: ip address of the vRouter
        required: true
    login:
        type: string
        description: login of vRouter
        required: true
    password:
        type: string
        description: password the vRouter
        required: true
    cmd_path:
        type: string
        description: Command path of the configuration session
        required: true
    ikeGroup:
        type: string
        description: IKE Group 
        required: true
#    proposal:
#        type: integer
#        description: IKE proposal
#        required: true
    ikeVersion:
        type: integer
        description: IKE version 
        required: true
#    encryption:
#        type: string
#        description: encryption algorithm for IKE grop 
#        required: true
#    hashAlg:
#        type: string
#        description: hash algorithm for IKE grop
#        required: true
#    lifetime:
#        type: integer
#        description: lifetime of the IKE group
#        required: true

enabled: true

notify:
  on-complete:
    message: "\"@channel: Action succeeded.\""
    routes:
      - "slack"